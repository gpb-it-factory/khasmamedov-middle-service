#empty so far
server:
  port: 8081
  servlet:
    encoding:
      enabled: true
      charset: UTF-8
      force: true

spring:
  messages:
    encoding: UTF-8

backend-service-c:
  url: http://localhost:8081/mock
